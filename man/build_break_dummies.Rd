% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/breaks.R
\name{build_break_dummies}
\alias{build_break_dummies}
\title{Step-dummy matrix for structural breaks}
\usage{
build_break_dummies(dates, break_dates = NULL)
}
\arguments{
\item{dates}{Vector of (daily) observation dates, coercible to \code{Date}.}

\item{break_dates}{Vector of break dates, or \code{NULL} for no breaks.}
}
\value{
Numeric matrix with \code{1 + length(break_dates)} rows and
\code{length(dates)} columns.
}
\description{
Builds the \verb{(1 + n_breaks) x TT} step-dummy matrix that turns the long-run
intercept and slopes into piecewise-constant series. Row 1 is the constant
regime; row \code{i + 1} switches on from break date \code{i} onwards. With
\code{break_dates = NULL} the matrix collapses to a single row of ones, so the
no-break model is simply the \code{n_breaks = 0} special case.
结构突变阶梯虚拟矩阵:第 1 行为常数基准,第 i+1 行从第 i 个突变日起为 1;
\code{break_dates = NULL} 时退化为单行全 1,无突变即 n_breaks = 0 的特例。
}
\examples{
days <- seq(as.Date("2019-01-01"), as.Date("2019-01-10"), by = "day")
build_break_dummies(days, as.Date("2019-01-06"))
build_break_dummies(days, NULL)  # no breaks -> single row of ones
}
