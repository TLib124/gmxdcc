% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gm-loglik.R
\name{gm_par_index}
\alias{gm_par_index}
\title{Parameter index map for the univariate GARCH-MIDAS model}
\usage{
gm_par_index(
  n_cov,
  n_regimes,
  distribution,
  cov_names = paste0("X", seq_len(n_cov))
)
}
\arguments{
\item{n_cov}{Number of long-run covariates \code{J}.}

\item{n_regimes}{Number of regimes \code{R} (\code{1 + n_breaks}).}

\item{distribution}{\code{"norm"} or \code{"std"}.}

\item{cov_names}{Optional covariate names used to label parameters.}
}
\value{
List with index vectors \code{alpha}, \code{beta}, \code{w2}, \code{nu} (or \code{NULL}),
\code{m}, \code{theta} (list of length \code{J}), \code{mu}, total length \code{n_par}, and the
canonical \code{names} vector.
}
\description{
Fixes the canonical parameter ordering used by \code{\link[=gm_loglik]{gm_loglik()}},
\code{\link[=gm_components]{gm_components()}} and the fitting driver:
\verb{alpha, beta, w2_1..w2_J, (nu,) m_1..m_R, theta_1_1..theta_J_R, mu}
where \code{J} is the number of long-run covariates and \code{R = 1 + n_breaks} the
number of regimes. 统一的参数排序约定:短期 α/β、各协变量权重 w2、
(std 分布的自由度 ν、)分段截距 m、各协变量分段斜率 θ、均值 μ。
}
\keyword{internal}
