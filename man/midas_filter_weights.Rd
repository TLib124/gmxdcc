% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/midas-weights.R
\name{midas_filter_weights}
\alias{midas_filter_weights}
\title{Rolling-sum weight vector for the MIDAS filter}
\usage{
midas_filter_weights(K, w2, lag_fun)
}
\arguments{
\item{K}{MIDAS lag order.}

\item{w2}{Second weighting parameter.}

\item{lag_fun}{\code{"Beta"} or \code{"Almon"}.}
}
\value{
Numeric weight vector of length \code{K + 1}.
}
\description{
Converts the raw lag weights into the vector expected by
\code{roll::roll_sum()} on a \verb{(K+1) x N} lag matrix: weights are reversed
(oldest row first), the current period is excluded (trailing zero), and
\code{w1} is fixed at 1 (Beta) or 0 (Almon) exactly as in the legacy code.
将原始权重转为 roll_sum 使用的形式:反转顺序、剔除当期(末位补 0),
w1 按 lag_fun 固定为 1(Beta)或 0(Almon),与旧脚本一致。
}
\keyword{internal}
