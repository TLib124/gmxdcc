% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gm-components.R
\name{gm_components}
\alias{gm_components}
\title{Variance components of a fitted GARCH-MIDAS model}
\usage{
gm_components(param, data)
}
\arguments{
\item{param}{Numeric parameter vector ordered as in \code{\link[=gm_par_index]{gm_par_index()}}.}

\item{data}{Data bundle as in \code{\link[=gm_loglik]{gm_loglik()}}.}
}
\value{
List of xts series \code{h_it_2}, \code{g_it}, \code{tau_d} indexed like
\code{data$daily_ret}.
}
\description{
Computes the daily long-run component \code{tau_d}, the short-run component
\code{g_it} and the total conditional variance \code{h_it_2 = g_it * tau_d} at a
given parameter vector, replacing the twelve branches of the legacy
\code{garchmidas_noskew_est()}. The recursion is a pure filter: when \code{data}
spans in-sample plus out-of-sample days, the out-of-sample segment is
automatically warm-started from the last in-sample state (no cold reset),
which is the basis of the package's out-of-sample design.
计算长期/短期成分与总条件方差;作为纯滤波器,传入全样本数据时
样本外段自然从样本内末期状态热启动,无冷重置。
}
\keyword{internal}
