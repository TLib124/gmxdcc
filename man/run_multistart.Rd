% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optim-driver.R
\name{run_multistart}
\alias{run_multistart}
\title{Multi-start constrained maximum-likelihood driver}
\usage{
run_multistart(
  loglik_fn,
  data,
  spec,
  n_starts = 100,
  seed = NULL,
  control = list()
)
}
\arguments{
\item{loglik_fn}{Per-observation log-likelihood, called as
\code{loglik_fn(param, data)}.}

\item{data}{Data bundle forwarded to \code{loglik_fn}.}

\item{spec}{Parameter specification from \code{\link[=gm_param_spec]{gm_param_spec()}} (or the DCC
analogue): supplies names, start ranges and \code{ui}/\code{ci} constraints.}

\item{n_starts}{Number of random starts (legacy default 100).}

\item{seed}{Optional RNG seed for the start draw.}

\item{control}{List; recognized fields: \code{start_matrix} (fixed candidate
matrix in canonical parameter order), \code{iterlim} (default 1000),
\code{method} (default \code{"BFGS"}).}
}
\value{
List with \code{est} (the \code{maxLik} object), \code{start_matrix}, and
\code{start_used} (the selected start vector).
}
\description{
Reproduces the legacy estimation scheme shared by every fit branch:
draw a matrix of candidate starts, evaluate the summed log-likelihood at
each, and run a single constrained BFGS \code{maxLik} from the best candidate.
New relative to the legacy script: an optional \code{seed} makes the draw
reproducible, and \code{control$start_matrix} injects a fixed start matrix
(used by the golden regression tests).
多起点估计驱动:生成候选初值、按似然择优、单次带约束 BFGS;新增
seed(旧脚本无种子不可复现)与 start_matrix 注入口(供回归测试)。
}
\keyword{internal}
