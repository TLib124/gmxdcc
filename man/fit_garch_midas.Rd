% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gm-fit.R
\name{fit_garch_midas}
\alias{fit_garch_midas}
\title{Fit a GARCH-MIDAS model with flexible covariates and structural breaks}
\usage{
fit_garch_midas(
  returns,
  X = NULL,
  K = 12,
  rv = is.null(X),
  K_rv = NULL,
  period = c("monthly", "quarterly"),
  distribution = c("norm", "std"),
  lag_fun = c("Beta", "Almon"),
  break_dates = NULL,
  out_of_sample = NULL,
  vol_proxy = NULL,
  n_starts = 100,
  seed = NULL,
  control = list()
)
}
\arguments{
\item{returns}{Daily return series (\code{xts}, one column).}

\item{X}{\code{NULL}, an \code{xts}, or a named list of low-frequency \code{xts}
covariates entering the long-run component.}

\item{K}{MIDAS lag order; scalar (recycled over \code{X}) or vector.}

\item{rv}{If \code{TRUE}, prepend internally generated realized volatility
(from \code{\link[=realized_measure]{realized_measure()}}) as a covariate. Default: \code{TRUE} when \code{X} is
\code{NULL}.}

\item{K_rv}{Lag order of the realized-volatility covariate (default
\code{K[1]}).}

\item{period}{Frequency of the long-run component: \code{"monthly"} or
\code{"quarterly"}.}

\item{distribution}{Innovation distribution, \code{"norm"} or \code{"std"}.}

\item{lag_fun}{MIDAS weighting scheme, \code{"Beta"} or \code{"Almon"}.}

\item{break_dates}{Optional \code{Date} vector of structural-break dates
segmenting \code{m} and all \code{theta_j} (\code{NULL} = no breaks).}

\item{out_of_sample}{Optional number of trailing observations to hold out.}

\item{vol_proxy}{Optional \code{xts} variance proxy for MSE/QLIKE loss
evaluation (default: squared returns).}

\item{n_starts}{Number of random optimizer starts (legacy default 100).}

\item{seed}{Optional RNG seed for reproducible starts.}

\item{control}{List of optimizer overrides: \code{bounds}, \code{start_matrix},
\code{iterlim}, \code{method}.}
}
\value{
An object of class \code{gm_fit}; see \code{\link[=summary.gm_fit]{summary.gm_fit()}}.
}
\description{
Two-component volatility model \eqn{r_t = \mu + \sqrt{\tau_t g_t}\,\epsilon_t}
where the short-run component follows a unit-mean GARCH(1,1) recursion and
the long-run component is
\deqn{\log \tau_t = m(t) + \sum_j \theta_j(t)\,\mathrm{MIDAS}(X_j)_t.}
Any number of low-frequency covariates is allowed, each with its own lag
order; \code{break_dates} makes the intercept \code{m} and all slopes \code{theta_j}
piecewise-constant. Estimation is constrained QMLE with random
multi-start; robust Bollerslev–Wooldridge sandwich standard errors are
reported (see \code{\link[=qmle_se]{qmle_se()}}).
}
\details{
Old model names map as: \code{GM_noskew_RV} = \code{rv = TRUE}; \code{GM_noskew_X} =
\code{X = list(x)}; \code{GM_noskew_RV_X} = both; the \verb{_sb} suffix = \code{break_dates}.

When \code{out_of_sample} is set, parameters are estimated on the first
\code{N - out_of_sample} days and the variance components for the held-out days
are obtained by \strong{filtering over the full sample with frozen parameters},
so the out-of-sample recursion is warm-started from the last in-sample
state. (The legacy script restarted the recursion cold at the first
out-of-sample day.)
样本外部分用冻结参数在全样本滤波后切片,从样本内末期状态热启动,
不再冷启动。
}
\examples{
\donttest{
# Market volatility driven by macro uncertainty, with structural breaks
# (bundled sample data; small n_starts to keep the example quick)
fit <- fit_garch_midas(
  topix_market["2005/2019"],
  X = list(JPMU = jp_macro_uncertainty), K = 12, rv = FALSE,
  break_dates = jp_mu_breaks[2:3],
  distribution = "norm", lag_fun = "Beta", period = "monthly",
  n_starts = 3, seed = 1
)
summary(fit)
coef(fit)
}
}
