% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tau.R
\name{build_tau}
\alias{build_tau}
\title{Long-run (MIDAS) variance component}
\usage{
build_tau(m_vec, theta, w2, lag_mats, K, D_mat, lag_fun)
}
\arguments{
\item{m_vec}{Numeric vector of regime intercepts, length \code{nrow(D_mat)}.}

\item{theta}{List (one element per covariate) of regime-slope vectors,
each of length \code{nrow(D_mat)}.}

\item{w2}{Numeric vector of second weighting parameters, one per covariate.}

\item{lag_mats}{List of \verb{(K_j + 1) x TT} covariate lag matrices (from
\code{\link[=midas_lag_matrix]{midas_lag_matrix()}}).}

\item{K}{Integer vector of MIDAS lag orders, one per covariate.}

\item{D_mat}{Step-dummy matrix from \code{\link[=build_break_dummies]{build_break_dummies()}}.}

\item{lag_fun}{\code{"Beta"} or \code{"Almon"}.}
}
\value{
Numeric vector \code{tau} of length \code{TT} (daily long-run variance).
}
\description{
The single builder of the daily long-run component shared by the
likelihood, the component extractor and (in its correlation flavour) the
DCC-MIDAS code. Implements
\deqn{\log \tau_t = m(t) + \sum_j \theta_j(t)\, \mathrm{MIDAS}(X_j; w_{2,j})_t}
where \eqn{m(t)} and every \eqn{\theta_j(t)} are piecewise-constant across
the regimes encoded in \code{D_mat}. Each old model variant (RV / X / RV_X,
with or without \verb{_sb}) is a special case with 1–2 covariates and 0+ breaks.
唯一的长期成分构造器:log tau = 分段截距 + 各协变量分段斜率×MIDAS 滤波;
旧模型 6 变体均为其 1–2 个协变量、0+ 突变的特例。
}
\keyword{internal}
