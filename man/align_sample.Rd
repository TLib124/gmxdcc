% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-data.R
\name{align_sample}
\alias{align_sample}
\title{Align daily returns to the coverage of a low-frequency series}
\usage{
align_sample(r_t, K = NULL, K_c = NULL, macro = NULL, period = NULL)
}
\arguments{
\item{r_t}{Daily xts series (returns).}

\item{K}{MIDAS lag order of the volatility covariates (may be \code{NULL}).}

\item{K_c}{MIDAS lag order of the correlation covariates (may be \code{NULL}).}

\item{macro}{Low-frequency xts covariate whose calendar defines coverage.}

\item{period}{\code{"monthly"} or \code{"quarterly"}.}
}
\value{
The trimmed daily xts series.
}
\description{
Trims a daily return series so that every retained day has at least
\code{max(K, K_c) + 1} complete low-frequency periods of covariate history
available before it. Direct port of the legacy \code{cut_func()}.
}
\examples{
# Keep only days with 12 full months of macro history available
# 只保留具备 12 个月完整宏观历史的交易日
x <- align_sample(topix_market, K = 12, macro = jp_macro_uncertainty,
                  period = "monthly")
range(zoo::index(x))
}
