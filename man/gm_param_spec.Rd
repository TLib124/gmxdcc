% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param-spec.R
\name{gm_param_spec}
\alias{gm_param_spec}
\title{Parameter specification for the univariate GARCH-MIDAS optimizer}
\usage{
gm_param_spec(
  n_cov,
  n_regimes,
  distribution,
  lag_fun,
  cov_names = paste0("X", seq_len(n_cov)),
  mu_start = 0,
  bounds = list()
)
}
\arguments{
\item{n_cov}{Number of long-run covariates \code{J}.}

\item{n_regimes}{Number of regimes \code{R = 1 + n_breaks}.}

\item{distribution}{\code{"norm"} or \code{"std"}.}

\item{lag_fun}{\code{"Beta"} or \code{"Almon"}.}

\item{cov_names}{Covariate names for parameter labelling.}

\item{mu_start}{Fixed starting value for \code{mu} (legacy: in-sample mean).}

\item{bounds}{Optional named list overriding entries of the default
bounds table.}
}
\value{
List with \code{idx}, \code{names}, \code{ui}, \code{ci}, \code{n_par} and the start-value
description used by \code{\link[=draw_starts]{draw_starts()}}.
}
\description{
Assembles, from the model dimensions alone, everything the optimizer
needs: canonical parameter names and index map (\code{\link[=gm_par_index]{gm_par_index()}}),
random-start ranges, and the \code{maxLik} inequality constraints
\code{ui \%*\% theta + ci >= 0}. Replaces the 21 hand-written legacy fit
branches. 由模型维度自动组装参数名、初值区间与不等式约束,
取代旧脚本 21 个手写分支。
}
\keyword{internal}
