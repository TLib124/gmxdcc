% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dcc-spec.R
\name{dcc_par_index}
\alias{dcc_par_index}
\title{Parameter index map for the DCC(-MIDAS) correlation step}
\usage{
dcc_par_index(
  n_assets,
  rc,
  n_X,
  n_regimes,
  X_names = paste0("X", seq_len(n_X))
)
}
\arguments{
\item{n_assets}{Number of assets \code{N}.}

\item{rc}{Does the long-run component include the realized-correlation
term?}

\item{n_X}{Number of exogenous correlation covariates \code{J}.}

\item{n_regimes}{Number of regimes \code{R}.}

\item{X_names}{Covariate names for labelling.}
}
\value{
List of index vectors plus \code{n_par} and \code{names}.
}
\description{
Canonical ordering, chosen to coincide with the legacy layout for every
configuration it supported:
\itemize{
\item no breaks, covariates present: \verb{a, b, (w2_rc,) delta_1..P, theta_<X>_1..P, w2_<X>_1..P, (theta_rc_1..P)}
\item breaks: \verb{a, b, (w2_rc,) w2_<X>_1..P, m_1..R, (theta_rc_1..R,) theta_<X>_1..R}
\item pure realized-correlation model without breaks: \verb{a, b, w2_rc} only.
}
}
\details{
\code{P = N(N-1)/2} asset pairs, \code{R = 1 + n_breaks} regimes. Without breaks the
level/slope coefficients are pair-specific; with breaks they are shared
across pairs but regime-varying, while the \code{w2} weights stay
pair-specific — exactly the legacy design.
相关步参数索引:无突变时系数逐资产对,有突变时跨对共享、按区制变化
(w2 始终逐对),与旧脚本设计一致。
}
\keyword{internal}
