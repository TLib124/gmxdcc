% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/midas-weights.R
\name{midas_weights}
\alias{midas_weights}
\title{MIDAS lag weighting functions}
\usage{
midas_weights(k, K, w1, w2, lag_fun = c("Beta", "Almon"))
}
\arguments{
\item{k}{Integer vector of lag positions, typically \code{1:K}.}

\item{K}{Total number of lags.}

\item{w1}{First weighting parameter. In the restricted Beta scheme used by
the models in this package \code{w1 = 1} (monotonically decaying weights);
for the Almon scheme \code{w1 = 0}.}

\item{w2}{Second weighting parameter.}

\item{lag_fun}{\code{"Beta"} or \code{"Almon"}.}
}
\value{
Numeric vector of weights of the same length as \code{k}, summing to 1
over \code{1:K}.
}
\description{
Beta and exponential-Almon lag weights, numerically identical to
\code{rumidas::beta_function()} and \code{rumidas::exp_almon()} (reimplemented here
to avoid the dependency; equality is checked in the test suite).
}
\examples{
# Decaying Beta weights over 12 lags / 12 阶递减 Beta 权重
midas_weights(1:12, 12, w1 = 1, w2 = 3, lag_fun = "Beta")
# Exponential Almon / 指数 Almon 权重
midas_weights(1:12, 12, w1 = 0, w2 = -0.05, lag_fun = "Almon")
}
