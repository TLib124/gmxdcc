% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dcc-fit.R
\name{univ_stage}
\alias{univ_stage}
\title{First (univariate) estimation stage}
\usage{
univ_stage(
  r_al,
  univariate,
  distribution,
  out_of_sample,
  univariate_args,
  r_full,
  period,
  lag_fun,
  n_starts,
  seed,
  control,
  vol_proxy = NULL
)
}
\arguments{
\item{r_al}{Aligned multivariate xts of daily returns.}

\item{univariate}{Model name (rugarch family or \code{"garch_midas"}).}

\item{distribution}{\code{"norm"} or \code{"std"}.}

\item{out_of_sample}{Number of held-out observations (or \code{NULL}).}

\item{univariate_args}{List of arguments for the \code{garch_midas} path:
\code{X}, \code{K}, \code{rv}, \code{K_rv}, \code{break_dates} (same semantics as
\code{\link[=fit_garch_midas]{fit_garch_midas()}}); \code{rv} covariates are computed from \code{r_full}.
Optionally \code{start_matrix}: optimizer starts for the first step — one
matrix used for every asset, or a list with one matrix per asset.}

\item{r_full}{Original (untrimmed) returns, used to compute realized
volatility with full history.}

\item{period, lag_fun, n_starts, seed, control}{Passed to \code{\link[=fit_garch_midas]{fit_garch_midas()}}.}

\item{vol_proxy}{Optional multivariate variance proxy.}
}
\value{
List with \code{u_est}, \code{est_details}, \code{u_loglik}, \code{u_inf}, \code{sd_full}
(TT x N matrix), \code{db_demeaned} (TT x N matrix).
}
\description{
Fits one volatility model per asset and returns everything the
correlation stage needs: full-sample conditional standard deviations
(in-sample fit, out-of-sample continued by each model's own filter),
demeaned returns, per-asset coefficient tables and log-likelihoods.
第一步:逐资产估计波动率模型,输出全样本条件标准差(样本外由各自
滤波器延续)、去均值收益及各资产结果。
}
\keyword{internal}
