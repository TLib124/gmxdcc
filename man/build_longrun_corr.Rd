% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dcc-longrun.R
\name{build_longrun_corr}
\alias{build_longrun_corr}
\title{Long-run correlation component of the DCC-MIDAS model}
\usage{
build_longrun_corr(param, dc)
}
\arguments{
\item{param}{Parameter vector in \code{\link[=dcc_par_index]{dcc_par_index()}} order.}

\item{dc}{Data bundle: \code{res} (TT x N residual matrix), \code{n_assets}, \code{TT},
\code{K_c}, \code{N_c}, \code{rc}, \code{X_mats} (list of \verb{(K_c+1) x TT} lag matrices),
\code{D_mat}, \code{lag_fun}, \code{idx}.}
}
\value{
\verb{N x N x TT} array \code{R_t_bar} (diagonal handling is left to the
caller, as in the legacy code).
}
\description{
Single builder of the slowly-moving correlation target \code{R_t_bar},
replacing the six legacy \code{dccmidas_noskew_loglik}/\code{mat_est} branches. The
functional forms follow the legacy code exactly:
\itemize{
\item realized-correlation only, no breaks: \code{R_bar} is the MIDAS-weighted
rolling average of the realized correlation matrices (no \code{tanh});
\item otherwise \code{R_bar = tanh(z)} with
\verb{z = level + sum_j slope_j * MIDAS(X_j) (+ slope_rc * RC)}, where the
level/slopes are pair-specific without breaks and regime-varying
(common across pairs) with breaks; \code{w2} weights are always
pair-specific.
长期相关构造器:纯 RC 无突变为加权滚动平均(无 tanh);其余为
tanh(截距 + 各斜率×MIDAS 项),无突变时逐对、有突变时按区制分段。
}
}
\keyword{internal}
