% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-data.R
\name{midas_lag_matrix}
\alias{midas_lag_matrix}
\title{Build the MIDAS lag matrix of a low-frequency covariate}
\usage{
midas_lag_matrix(x, mv, K, period)
}
\arguments{
\item{x}{Daily xts series that defines the target calendar.}

\item{mv}{Low-frequency xts covariate.}

\item{K}{MIDAS lag order (number of low-frequency lags).}

\item{period}{One of \code{"weekly"}, \code{"monthly"}, \code{"quarterly"}, \code{"yearly"}.}
}
\value{
A numeric \verb{(K+1) x N} matrix of stacked covariate lags.
}
\description{
For every high-frequency observation in \code{x}, stacks the current and \code{K}
previous low-frequency values of \code{mv} into a \verb{(K+1) x N} matrix, where
\code{N = length(x)}. Direct port of the legacy \code{mv_into_mat2()}.
}
\examples{
x <- align_sample(topix_market, K = 12, macro = jp_macro_uncertainty,
                  period = "monthly")
M <- midas_lag_matrix(x, jp_macro_uncertainty, K = 12, period = "monthly")
dim(M)  # (K+1) x length(x)
}
