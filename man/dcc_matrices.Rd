% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dcc-components.R
\name{dcc_matrices}
\alias{dcc_matrices}
\title{Conditional correlation and covariance matrices of a fitted DCC model}
\usage{
dcc_matrices(param, model, data, D_t, S_in = NULL, N_in = NULL)
}
\arguments{
\item{param}{Estimated parameter vector.}

\item{model}{One of \code{"cDCC"}, \code{"aDCC"}, \code{"DECO"}, \code{"DCC-MIDAS"}.}

\item{data}{DCC data bundle (see \code{\link[=build_longrun_corr]{build_longrun_corr()}}); for the
non-MIDAS models only \code{res} and \code{K_c} are used.}

\item{D_t}{\verb{N x N x TT} cube of conditional-standard-deviation diagonal
matrices.}

\item{S_in}{Optional unconditional covariance target (in-sample).}

\item{N_in}{Optional aDCC asymmetric target (in-sample).}
}
\value{
List with \code{H_t}, \code{R_t}, and for DCC-MIDAS also \code{R_t_bar}.
}
\description{
Runs the correlation recursion at fixed parameters and returns the
\code{H_t}, \code{R_t} (and \code{R_t_bar} for DCC-MIDAS) arrays, replacing the legacy
\code{dccmidas_noskew_mat_est()} plus the direct \verb{new_*_mat_est} calls.
}
\details{
The function is a pure filter: called on the full (in-sample plus
out-of-sample) residual series with parameters estimated in-sample, the
out-of-sample segment continues seamlessly from the last in-sample state.
Pass \code{S_in} (and \code{N_in} for aDCC) computed from in-sample residuals so
the unconditional targets carry no look-ahead — this replaces both the
legacy cold restart and its \code{N_c_res_for_oos} patch mechanism.
纯滤波器:对全样本残差与冻结参数运行,样本外自然热启动;S_in/N_in
传入样本内目标以消除前视,取代旧的冷启动与 N_c_res_for_oos 补丁。
}
\keyword{internal}
