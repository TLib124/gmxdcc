% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-stats.R
\name{inf_criteria}
\alias{inf_criteria}
\title{Information criteria for a fitted model}
\usage{
inf_criteria(est)
}
\arguments{
\item{est}{A fitted object of class \code{uGARCHfit}, \code{maxLik}, or \code{gm_fit}.}
}
\value{
A named numeric vector with elements \code{AIC} and \code{BIC}.
}
\description{
Computes AIC and BIC for a \code{uGARCHfit} (rugarch), \code{maxLik}, or \code{gm_fit}
object (see \code{\link[=fit_garch_midas]{fit_garch_midas()}}). For \code{gm_fit} objects the criteria
stored at fit time are returned directly when available.
}
\examples{
set.seed(1)
y  <- rnorm(300, 1, 2)
ll <- function(p) stats::dnorm(y, p[1], exp(p[2]), log = TRUE)
fit <- maxLik::maxLik(ll, start = c(mu = 0, logsd = 0), method = "BFGS")
inf_criteria(fit)
}
