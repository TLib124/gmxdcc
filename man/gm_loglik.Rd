% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gm-loglik.R
\name{gm_loglik}
\alias{gm_loglik}
\title{Per-observation log-likelihood of the GARCH-MIDAS model}
\usage{
gm_loglik(param, data)
}
\arguments{
\item{param}{Numeric parameter vector ordered as in \code{\link[=gm_par_index]{gm_par_index()}}.}

\item{data}{List bundle with elements \code{daily_ret} (xts), \code{lag_mats},
\code{K}, \code{D_mat}, \code{lag_fun}, \code{distribution}, \code{idx}.}
}
\value{
Numeric vector of per-observation log-likelihood contributions.
}
\description{
Replaces the twelve model-by-distribution branches of the legacy
\code{garchmidas_noskew_loglik()}: the long-run component comes from the single
generalized builder \code{\link[=build_tau]{build_tau()}}, the short-run recursion and likelihood
from the C++ kernels. Returns the per-observation vector required by
\code{maxLik} for OPG/sandwich standard errors.
取代旧脚本 12 个分支的统一似然:长期成分走 build_tau,短期递推与似然
走 C++ 核心;逐观测返回以便 maxLik 计算三明治标准误。
}
\keyword{internal}
